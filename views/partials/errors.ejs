<ul>
    <% if (errors.length > 0) { %>
        <h3>Oops! There was at least one error. See below:</h3>
        <% errors.forEach(error => { %>
            <li>
                <%= error.msg %>
            </li>
        <% }) %>
    <% } %>
</ul>